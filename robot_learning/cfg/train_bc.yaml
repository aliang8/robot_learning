num_updates: 20_000
log_terminal_every: 2_000 
run_eval_rollouts: True
log_rollout_videos: True

data: 
  data_type: transitions

use_separate_gripper: False
gripper_pos_weight: 0.1

defaults:
  - base
  - model/policy@model: mlp
  - _self_